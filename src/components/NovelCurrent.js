import React, { Component } from 'react';
import { Text } from 'react-native';
import { Card } from './common';

class NovelCurrent extends Component {
  render() {
    return (
      <Card>
        <Text> Novel item</Text>
      </Card>
    );
  }
}

export default NovelCurrent;
